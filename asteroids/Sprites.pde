void shipHandler(){
  // if rotate key(s) pressed, rotate ship
  // if thruster pressed, apply thruster
  // limit speed to shipMaxSpeed
}

void bulletHandler(){
  // if shoot key pressed, add bullet instance to bulletArray
  // check current bulletStyle and bulletSpeed(changes as per upgrade system)
  // Save direction and initial location value to match the ship
  // Move and Render all bullets in the bulletArray as per speed/angle etc.
  
}

void enemyHandler(){
  // iterate through enemy array and update their positions
  // movement can be simple, just linear and “wrap” around screen edges
  // as per original game
}
