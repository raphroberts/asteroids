void titleScreen() {
  // drawBackground(index);
  // Set gameScore var to 0
  // Set the following to default values:
  // enemySpeed, numberOfEnemies, 
  // shipMaxSpeed, shipTurnSpeed, shipGraphic
  // shipLocation, shipVelocity, shipAcceleration
  // bulletSpeed, bulletGraphic, bulletStyle
  // set background to title screen image
  // place “start” button
  // when “start” selected: call populateEnemiesArray(); call noCursor(); 
  // and change currentScreen (game screen)
}
